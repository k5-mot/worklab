# suggested commands
- `rg --files doc` to list documentation files quickly.
- `rg -n "pattern" doc` to search docs.
- `nl -ba <file>` to inspect Markdown with line numbers.
- `docker compose -f infra/1-alfa/docker-compose.yml config` to validate alfa compose.
- `docker compose -f infra/2-bravo/docker-compose.yml config` to validate bravo compose.
- Standard Linux utilities available: `git`, `ls`, `find`, `sed`, `cat`, `bash`.
- No project-specific formatter/linter/test command was discovered from root manifests during onboarding.