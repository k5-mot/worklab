# worklab overview
- Purpose: manage an on-premise developer platform as grouped stacks.
- Main content areas: `doc/` for docs-as-code, `infra/` for stack-specific infrastructure definitions.
- Infrastructure currently includes `infra/1-alfa/docker-compose.yml` and `infra/2-bravo/docker-compose.yml`.
- Documentation is organized into `doc/spec`, `doc/design`, and `doc/rules`.
- Primary language/config observed: Markdown and Docker Compose YAML; repo also contains bash-oriented workflow context.