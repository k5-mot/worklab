# style and conventions
- Documentation follows docs-as-code rules in `doc/rules/document-style.md`.
- `doc/spec/*` is used for current WHAT/living specs.
- `doc/design/*` is used for WHY/design decisions.
- `doc/glossary.md` is the single source of truth for domain term definitions.
- Repository uses concise Markdown with headings and bullet lists.